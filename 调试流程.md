# ModelSim 调试流程

## Blade 双帧缓冲：PC 快照经验

对 `soc_top_lv3_board2x2.sv` 的慢仿真，先做 10us、1ms、10ms 的短快照：DDR ready → CPU PC → GDU `scan_run_pix`/`swap_pending` → DVI frame。PC 若在相邻地址反复跳转，立刻在 `obj/<example>.s` 映射地址。

本项目中 `0x1c0007d8/0x1c0007e0` 对应 `gdu_fb_wait_present_done_checked()` 的轮询。再配合 `scan_active_sys`、`present_pending` 与 `present_done_evt`，可直接判断是 swap 后扫描未重启，而不是软件或 DDR 未启动。

Blade TB 应打印 reset、DDR、UART banner、每 1ms 的 PC/scan/swap/frame 心跳以及每个 PPM 路径；使用 `+UART_ECHO` 将 UART 同时写入 transcript。

## 1. 适用场景

以“新增一个 RTL 功能，并在 ModelSim 中完成联调”为主线。
默认走 `gru_gdu_animation_tb` 这类带软件程序的 SoC 仿真；如果问题已缩小到图形子系统，也可切到 `gru_gdu_local_tb`。

## 2. 须知规范

- 自动化调试只用 `make -C fpga/modelsim ... run-batch`，不要用 `run` GUI 模式。
- 结论以 batch transcript 和 testbench 的 `$finish` / `$fatal` 为准。
- `fpga/modelsim/build/`、`fpga/modelsim/logs/` 是运行产物目录，不要手工长期维护。
- 默认每次仿真前会清空 `sim/frame_output/`；如需保留旧抓帧，追加 `CLEAN_FRAME_OUTPUT=0`。
- `rtl/config_common.h` 只放公共宏、路径宏、基地址等，不定义最终分辨率。
- 系统级分辨率/时序/输出模式由 `rtl/config_remote.h` 或 `rtl/config_complex.h` 覆盖。
- `rtl/config_sim.h` 只保留给轻量 demo / 局部 TB。

## 3. 先改哪些

新增 RTL 功能时，通常要同步检查这几类文件：

- RTL 主逻辑：`rtl/ip/...` 下对应模块，例如 `*_top.sv`、`*_cmd_decoder.sv`、对应 engine。
- 仿真入口：`sim/test/*.sv`。
- ModelSim filelist：`fpga/modelsim/filelist/tb_*.f`。新增 testbench 时必须补。
- 软件调用侧：`sdk/software/bsp/drivers/*.c` 和对应 example 的 `main.c`。
- 必要时参考模型：`sim/model/*.sv`。

## 4. 配置对应关系

RTL 侧：

- `rtl/config.h`：统一入口；系统级优先走 `remote / complex`，无 profile 时仍默认 `config_sim.h` 供轻量 TB 使用。
- `rtl/config_sim.h`：轻量仿真配置，当前为 `80x60`，并口 LCD。
- `rtl/config_remote.h`：远程系统配置，当前为 `400x300` framebuffer，GDU 2x 输出 `800x600`。
- `rtl/config_complex.h`：复杂系统配置，当前为 `800x480` framebuffer，GDU 1x 输出 `800x480`。
- `rtl/config_common.h`：公共宏、`axi_ram.mif` 路径、frame output 路径等。

软件侧：

- `sdk/software/bsp/include/soc_config_common.h`
- `sdk/software/bsp/include/soc_config_sim.h`
- `sdk/software/bsp/include/soc_config_remote.h`
- `sdk/software/bsp/include/soc_config_complex.h`

要求：

- 如果你改了 framebuffer 尺寸、stride、像素格式或板级/仿真行为，RTL 和软件两侧配置要一起对齐。
- 当前仓库已保证 `rtl/config.h`、`rtl/config_remote.h`、`rtl/config_complex.h`、`rtl/config_sim.h`、`rtl/config_common.h` 的 SoC 调用链可用；新增功能时不要破坏这层对应关系。

## 5. 软件编译要求

进入 WSL，在目标程序目录执行：

```bash
cd sdk/software/examples/gru_gdu_animation
make clean
make
```

编译结果：

- 本地中间产物在 `sdk/software/examples/<example>/obj/`
- `make` 会生成 `axi_ram.mif`
- `common.mk` 会把 `obj/axi_ram.mif` 复制到 `sdk/axi_ram.mif`

因此，跑 ModelSim 前要先确认目标 example 已重新编译，否则仿真仍可能加载旧程序。

若同一阶段需要先后保留多个软件镜像，不要让所有 TB 都隐式加载公共 `sdk/axi_ram.mif`；`common.mk` 会被最后一次软件构建覆盖。做法是让对应 TB/top 显式引用各 example 自己的 `obj/axi_ram.mif`，或在每次仿真前重新构建目标 example。

## 6. ModelSim 调试主线

1. 修改 RTL / 软件 / testbench。
2. 进入 WSL，重新编译目标 example，生成新的 `sdk/axi_ram.mif`。
3. 运行仿真：

```powershell
make -C fpga/modelsim TB=gru_gdu_animation_tb run-batch
```

4. 如果只想验证本地图形链路，可改用：

```powershell
make -C fpga/modelsim TB=gru_gdu_local_tb run-batch
```

5. 需要波形时再开 VCD：

```powershell
make -C fpga/modelsim TB=gru_gdu_animation_tb VCD=1 VCD_CFG=lite run-batch
```

## 7. 常见注意点

- 仿真编译入口是 `fpga/modelsim/Makefile`，不要直接手改 `build/` 里的内容。
- 新 testbench 没进 `filelist/tb_*.f`，ModelSim 不会编进去。
- 软件改了但没重新 `make`，`sdk/axi_ram.mif` 不会更新。
- 若改的是远程系统显示参数，检查 `config_remote.h` 与 `soc_config_remote.h` 是否一致。
- 若改的是复杂系统显示参数，检查 `config_complex.h` 与 `soc_config_complex.h` 是否一致。
- 若改的是轻量 demo 显示参数，检查 `config_sim.h` 与 `soc_config_sim.h` 是否一致。

## 8. 结果判读

一次 `run-batch` 跑完后，按下面顺序看结论。

1. **先看 `fpga/modelsim/logs/result_<tb>.json`**（由 `scripts/build_result_json.py` 生成）。关键字段：

   | 字段 | 含义 |
   |---|---|
   | `status` / `pass_fail` | 总体 `pass` / `fail` |
   | `compile_ok` / `load_ok` / `run_ok` | 编译 / 加载 / 仿真是否成功 |
   | `samples.pass_marker` | TB 打印的通过检查点（如 `[GRU_GDU_TEXT_TB] horizontal_motion_step0 PASS`） |
   | `samples.fail_marker` | TB 打印的失败点 |
   | `known_noise_counts` | 已知噪声计数（见下，不计故障） |
   | `warning_counts` | 非噪声告警分类计数 |
   | `vcd_path` / `selected_cfg` / `capture_mode` | 本次波形抓取情况 |

2. **没有 result JSON 时**，直接看 `logs/transcript_<tb>.log`：TB 用 `$fatal`/`$finish` 提前退出会跳过 `build_result_json.py`（见 §9）。搜 `PASS` / `FAIL` / `Fatal` / `Error`，以及 TB 自打的检查点。

3. **已知噪声**（`vcdcfg/manifest.yaml` 的 `noise_rules` 归类，都是 t=0 的固有告警，不算故障）：

   - `ddr_missing_model_data`：`no +model_data option specified`
   - `ddr_reset_timing`：`200 us is required before RST_N goes inactive`
   - `ddr_cke_reset`：`CKE must be inactive when RST_N goes inactive`
   - `vsim_suppressible_division`：`(vsim-8630) Infinity results from division operation`

4. **端口宽度告警** `(vsim-3015) [PCDPC] ... port 'level' ... (8) ... (5)`（`rtl/ip/gru/gru_fifo.sv`）目前是已知的，不阻塞仿真。

## 9. 常见故障排查

- **看门狗超时 `[..._TB] timeout after ...`**：TB 里 `parameter time TB_TIMEOUT = 50ms`（如 `gru_gdu_blit_text_motion_tb` / `gru_gdu_blit_contention_tb`），跑到 `#TB_TIMEOUT` 还没结束就 `$fatal`。先分清是真挂死还是看门狗太短——这类多步骤 blit suite 正常需要 ~99ms，50ms 会误杀。把 `TB_TIMEOUT` 临时调到 500ms 重跑：能跑完就是看门狗问题，依旧超时就是真挂死。
- **死锁（等 DONE 永远等不到）**：典型是 `gru_setup` / `gdu_setup` 之后立刻 `gru_wait_idle_checked` 等 `GRU_STATUS_DONE`，但 `status_done` 只在 busy→idle 下沿置位，setup 没发命令就永远等不到，一直转到看门狗。DONE 等待要放在真正 issue 引擎命令之后。
- **`result_<tb>.json` 没生成**：`$finish` / `$fatal` 提前退出会跳过 `run_batch.do` 末尾的 `build_result_json.py`。手动补一份：

  ```bash
  python fpga/modelsim/scripts/build_result_json.py \
    --transcript fpga/modelsim/logs/transcript_<tb>.log \
    --result fpga/modelsim/logs/result_<tb>.json \
    --manifest fpga/modelsim/vcdcfg/manifest.yaml \
    --tb <tb> --compile-ok 1 --load-ok 1 --run-ok 1 \
    --selected-cfg lite --capture-mode disabled
  ```

- **抓帧（`.ppm`）目录**：`lcd_monitor` 默认按 `DVI_FRAME_OUTPUT_ROOT`（`rtl/config_common.h`，当前 `"../../../sim/frame_output"`，相对 `fpga/modelsim/` 解析）写帧。正常情况下默认就正确（如本次 `gru_gdu_blit_*` 的帧都落在了 `sim/frame_output/`）。只有当该路径 `$fopen` 失败时，monitor 会打 `[LCD_MON][WARN] fallback to current simulation directory` 并退回 cwd（`build/`）；此时给 `VSIM_RUN_FLAGS` 显式指定绝对路径即可：

  ```bash
  make -C fpga/modelsim TB=<tb> \
    VSIM_RUN_FLAGS='+acc +LCD_OUT_DIR=D:/Course/jichuangsai/fpga/Blade-of-Chip/sim/frame_output' \
    run-batch
  ```

  （`dvi_monitor` 用的是 `+DVI_OUT_DIR`，名字不同；仿真并口 LCD 走 `lcd_monitor` → `+LCD_OUT_DIR`。）

- **新 TB 没编进去**：必须新建 `fpga/modelsim/filelist/tb_<tb>.f`，否则 Makefile 找不到文件列表。
- **新 TB 找不到 harness include（`Cannot open '../gru_graph_system_tb_harness.svh'` 之类）**：`sim/test/3d/` 下的 TB 用 `include "../gru_graph_system_tb_harness.svh"`，对应 `tb_<tb>.f` 必须加 `+incdir+../../../sim/test`（compile.do 是 `cd $FILELIST_DIR` 后 `vlog -f`，路径都以 `fpga/modelsim/filelist/` 解析）。phase0/1/2/3/cube 的 filelist 都有这行，新 TB 直接照抄三行写法（`-f base.f` / `+incdir+../../../sim/test` / TB 源路径）即可。
- **SV 关键字当变量名（`vlog-13069`）**：用 SystemVerilog 保留字做变量/数组名会报 `(vlog-13069) <file>(<ln>): near "X": syntax error, unexpected "SystemVerilog keyword 'X'"`，常连带 `unexpected '['` / `unexpected end`。`.sv` 按 SV 模式编译时这些字一律保留（`.v`/Verilog 模式部分不保留，同段代码换扩展名或编译模式可能不报错，更具迷惑性）。最易当普通词误用的：`expect`（SVA，phase5 TB 踩中）、`do`、`final`、`type`、`string`、`event`、`cover`、`cross`、`bins`、`restrict`、`assume`、`throughout`、`within`、`intersect`、`matches`、`implies`、`first_match`；`local`/`static`/`automatic`/`const`/`virtual`/`pure` 等修饰符同理。修法只有改名（SV 无实用的转义手段，`\name ` 可读性差不推荐），同步改声明处 + 全部引用处即可；新增 TB 后先 `make -C fpga/modelsim TB=<tb> compile` 跑一遍能在 `run-batch` 前拦住这类错。
- **软件改了没生效**：见 §5，`sdk/axi_ram.mif` 没重新 `make` 就不会更新。
- **vopt `Optimization failed`（无明细）多半是 TB 命名端口连到了不存在的端口**：模块端口改名或删端口后，若 TB 还用旧名 `.old_port(sig)` 实例化，ModelSim **vlog 不报错也不告警**（命名连接到不存在端口被静默忽略），但到 `vsim -voptargs="+acc"` 精细化阶段会打 `Design is being optimized... / Optimization failed / Error loading design`，且 transcript 不给具体原因。模块端口列表或定点格式一改，立刻 `grep -rn <旧端口名> rtl/ sim/` 把 TB 侧陈旧引用一起改掉。典型：phase6b 把 `gru_reciprocal` 输出 `reciprocal_q2_30` 改名 + Q2.30→Q4.28 时只更新了 raster 的 `recover_coord`，漏改 cube/phase5 两个 TB，latent 到 phase7 回归才暴露（phase6b 当时只回归了 phase0/1/2/3/6a，跳过了 cube/phase5）。
- **TB 检查寄存器输出要隔一拍**：被测模块用 `<=`（NBA）写出的状态位（dirty 标志、done 标志等）在某个 posedge 的 NBA 区才更新；TB 若在**同一个 posedge 的 active 区**读它（`@(posedge clk)` 返回后立刻 `if (!dut_dirty) $fatal`），读到的是更新前的旧值 → 误 fatal。寄存器输出必须在驱动它的那个沿**之后**再 `@(posedge clk)` 采样；组合输出（`assign`）才能同沿读。常见翻车点：写后查 dirty、clear 后查 dirty 是否清掉、issue 命令后查 done。
- **TB 的 PASS 与看门狗必须互斥**：PASS 路径先置 `done=1`，看门狗末尾必须写成 `if (!done) $fatal(...)`。不要依赖 `$finish` 后的语句来阻止看门狗；不同仿真器对终止请求同一时间步内的后续过程调度不同，可能出现 transcript 同时含 PASS 和 FAIL，结构化结果会按 FAIL 处理。
- **OS 类 TB 里“没看到 UART”不等于“UART 没发”**：这类 TB 主要靠 `uart_agent.uart_wait_tx_string(...)` 判定字符串是否出现，**不会像 `fpga_test_flash_tb` 那样把每个 UART 字符实时 `$write` 到 transcript**。所以 batch log 里一开始只看到 DDR 噪声、长时间看不到 `BLADE_OS READY` / `SCENE DESKTOP`，不能直接下结论说“操作系统没打印”或“UART 坏了”；要么等到 `uart_wait_tx_string` 成功/超时，要么临时加一个 APB/UART 字符回显 monitor 再看。
- **`fpga_test_flash_tb` 只能证明 UART 硬件路径可用，不能单独证明 RT-Thread/BladeOS 打印链路没问题**：它验证的是 `printf`/`uart_read_line` 这条裸机路径，至少能说明 `u_axi_uart_controller`、UART 寄存器访问、TB 的 `uart_agent` 发送接收链路是通的；但 BladeOS/RT-Thread 走的是 `rt_kprintf -> rt_hw_console_output -> uart_putchar`，中间还可能受线程启动、组件初始化、console hook、terminal display hook 影响。看到 `fpga_test_flash_tb` 有串口输出时，正确结论是“UART 基础通路大概率没坏”，下一步应继续排查 OS 是否已经跑到 `main()` / `INIT_APP_EXPORT()`、DDR 是否 ready、以及 TB 是否只是还没等到字符串。

## 10. 波形诊断（进阶）

默认 `VCD=0` 不抓波形；需要时再开。

- **三档预设**：`VCD_CFG=lite`（最小，默认）/ `debug` / `full`（最大，慢）。每档对应 `vcdcfg/<tb>.<cfg>.lst`；TB 没有该档的 `.lst` 就用不了（如 `gru_gdu_blit_contention_tb` 目前只有 `lite`）。

  ```bash
  make -C fpga/modelsim TB=<tb> VCD=1 VCD_CFG=lite run-batch
  ```

- **抓窗（推荐固定窗口，省体积）**：

  ```bash
  make -C fpga/modelsim TB=<tb> VCD=1 VCD_CFG=lite \
    VCD_START=70us VCD_END=85us run-batch
  ```

  或触发居中：`VCD_TRIGGER=<信号> VCD_PRE=... VCD_POST=...`。

- **按 phase 抓信号**：`PHASE=fence|backpressure|present`，会按 `manifest.yaml` 里该 phase 的 `profiles` + `snapshot_signals` 选信号；也可直接 `PROFILES=gru_writer,backpressure`。

- **单一时刻快照**（比重跑波形省事）：`make -C fpga/modelsim TB=<tb> snapshot SNAPSHOT_TIME=80us`，配合 `SNAPSHOT_SIGNALS_FILE` 或 phase 默认信号取状态切片。

- **不要把原始 `.vcd` 或整份 transcript 读进上下文**：用脚本做有界查询。仓库自带 `scripts/vcd_utils.tcl`（Tcl 侧）；AI 会话里可用 `modelsim-batch-debug` skill 的 `transcript_summarize.py` / `vcd_query.py` / `modelsim_snapshot.py`，顺序是先 `summary` / `signal.find`，再 `value.at` / `value.window`，每条查询都带信号过滤或时间范围。

## 11. Blade 2×2：先用快照定位慢仿真，而非盲等完整回归

Blade GUI 的软件启动会进行多页 GRU 绘制、GDU present 和 UART 初始化；完整 small TB 的仿真时间很长。遇到“没有 UART 输出 / 没有 PPM”时，先采 100 us、1 ms、5 ms、15 ms 等关键时刻的状态，按以下顺序判断：DDR ready → CPU PC 是否前进 → GDU enable / scan → swap 状态 → UART buffer → DVI monitor 行列计数。

```powershell
$env:MGLS_LICENSE_FILE='C:\Cdownload\modelsim\win64\LICENSE.TXT'
python skill/modelsim-batch-debug/scripts/modelsim_snapshot.py --tb blade_gui_2x2_small_tb --time 15ms `
  --signal /blade_gui_2x2_small_tb/dut/ddr_init_calib_complete `
  --signal /blade_gui_2x2_small_tb/dut/debug_wb_pc `
  --signal /blade_gui_2x2_small_tb/dut/u_gdu_top/swap_pending `
  --signal /blade_gui_2x2_small_tb/dut/u_gdu_top/u_gdu_regs/status_swap_done `
  --signal /blade_gui_2x2_small_tb/dut/u_gdu_top/scan_run_pix `
  --signal /blade_gui_2x2_small_tb/uart/u_monitor/tx_buffer `
  --signal /blade_gui_2x2_small_tb/mon/captured_frame_width `
  --signal /blade_gui_2x2_small_tb/mon/captured_frame_height `
  --extra-var 'MODELSIM_DEFINES=+define+MODELSIM_BUILD +define+RTL_USE_SIM_CONFIG +define+RTL_USE_BLADE2X2SIM_CONFIG'
```

一次实际定位中，CPU PC 停在 `0x1c0073f8/0x1c007400`，对应 `gdu_fb_wait_swap_done_checked()`；同时 GDU 读取状态为 `0x10`（仅 `swap_pending`），`status_swap_done=0`。这表明软件在等待 vblank 后的 swap 完成，而不是 UART 或 DDR 已失效。应继续检查 `swap_done_evt`、`vblank_evt_sys`、`gdu_regs.status_swap_done` 与 `gdu_regs.w_enter/req_addr`，确认事件是否被锁存或被重复的 swap 写清除。

若 PC 停在裸机 probe 的 `gdu_fb_wait_present_done_checked()`，但 `scan_run_pix=1`、DVI frame 和 GDU read counters 持续增长，说明显示扫描正常而 `GDU_STATUS_PRESENT_DONE` 没被锁存；优先查首帧 `present_done_evt`。

### Blade 的 UART、DVI 与 VCD 注意项

- `uart_agent` 默认只缓存 TX 字符；要把它显示到 transcript，使用 `+UART_ECHO`。
- 覆盖 `VSIM_RUN_FLAGS` 时必须保留优化可见性：`VSIM_RUN_FLAGS='-voptargs=+acc +UART_ECHO'`。只传 `+acc` 会让内部信号在 vopt 后不可查询。
- `modelsim_snapshot.py` 不会传 `+DVI_OUT_DIR`，故 monitor fallback 到当前目录的警告不代表正常 `run-batch` 的 dump 路径失效；完整 batch 会注入绝对输出目录。
- Blade 当前 `lite` VCD preset 的 `sim:/...` pattern 与 `vcd_utils.tcl` 的 `find signals` 路径约定不匹配时，应先通过 `vcd_cfg_expand.py` 建立 `build/ai_vcdcfg/` 下的临时配置，并使用 `/blade_gui_2x2_small_tb/...` 层级。诊断期间不要直接改 checked-in preset。

- Blade 2x2 首个 DVI PPM 有时处于 monitor 启动同步期，可能出现诸如 `320x1680` 的拼接高度；不要用 frame 1 判断时序或图像失败。以随后的完整 `320x240` 帧和 TB 最终尺寸检查为准。
- 页面重绘或 swap 附近也可能出现 `320x480` 的复合 PPM：`dvi_monitor` 漏过一帧边界后会把两帧行数拼在一起，而其像素缓存仍只有 `MAX_HEIGHT` 行。这不是 GUI 分辨率被拉伸；用相邻的 `320x240` 帧和最终断言验收。
- Blade 的 SW6/SW7 是 UI 线程每 `20 ms` 轮询的电平输入。TB 验证开关时应保持该电平直到收到 UART 事件，不能复用按键的短脉冲时序。

### Blade swap 后无 DVI 帧：检查 V_SYNC 是否被截断

若 PC、DDR、`scan_run_pix`、swap 和 UART 均正常，但 `dvi_monitor` 长期为
`frames=0 size=0x0`，对照有界 VCD 的 `timing_de_pix`、`video_de`、
`video_vsync` 与 `display_timing_gen.v_cnt`。DE 活跃而 V_SYNC 不翻转，表示
swap 在 `vblank_enter` 截断了 scanout；应让 timing 走完整个 blanking/V_SYNC，
只在 blanking 内刷新 reader/FIFO。
