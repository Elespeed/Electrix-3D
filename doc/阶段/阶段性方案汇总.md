
# 三种方案 [[初次论文投稿]] [[整改行动]]
- RT thread
- 完成性能计数
- 
```
① CPU_ONLY
CPU：
Transform
Cull
Sort
Command
全部自己干

        ↓

② CPU_MATMUL
矩阵变换 → 硬件
Cull
Sort
Command → CPU

        ↓

③ SCENE_CONTROLLER
CPU：
“这个模型，这个姿态，画一帧。”

硬件：
Transform
Cull
Sort
Command
全部完成
```

### 最后绘图要求

**模型规模 vs CPU active cycles/frame**

```
CPU_ONLY
CPU_MATMUL
SCENE_CONTROLLER
```

三条线。


**模型规模 vs P99 frame latency**

画一条：

```
33.333 ms
----------------
```

实时预算线。



**模型规模 vs RT-Thread idle rate / 后台任务吞吐量**

这个其实特别能体现你 Scene Controller 的意义：

> CPU不是单纯“少算了几周期”，而是真的释放出来给 RTOS 干别的事情。


# 统一测试
### 指标

直接用 Python 自动生成确定性的几何数据：

```
bench_S0.sk3d
bench_S1.sk3d
bench_S2.sk3d
bench_S3.sk3d
bench_S4.sk3d
```

严格保证：

```
S0 = 16V / 24T / 1M
S1 = 32V / 48T / 1M
...
```

保存：

```
SHA-256
V
T
M
文件大小
生成器版本
```

Benchmark 模型甚至长得像一团奇怪的刺猬都没关系
- 新要求不考虑mesh数影响

| 模型  | 顶点 V | 三角形 T | Mesh M |
| --- | ---: | ----: | -----: |
| S0  |   16 |    24 |      1 |
| S1  |   32 |    48 |      1 |
| S2  |   64 |    96 |      1 |
| S3  |   96 |   144 |      1 |
| S4  |  128 |   192 |      1 |
