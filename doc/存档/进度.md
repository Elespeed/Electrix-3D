> 此文档要求表述简洁
当前状态：
- T00：当前工作树无法复核文档声称的 `b224aa0`（本地仅有 `1f05176`、`602bb1f`）；不将“56/56”作为当前基线证据。
- T01：实验协议已完成，待与 T02/T03 一并提交。
- T03：JSONL 校验器和样例已复核：`valid.jsonl` 通过（2 帧），`invalid.jsonl` 按预期拒绝；`sketch_perf_tb` 的预检声明尚无当前日志可复核。
- T02：尚未冻结；资产清单 SHA-256=`E3011C4C0AF18E031554C96601EBD5A91DF72FC1F03E7AC5ED0822BC7088FA0B`。仍需补齐可复核 Git 基线、冻结元数据与资源/时序证据。
- T04--T07：已新增独立 `sdk/software/examples/rt_3d/` RT-Thread benchmark；固定 30 帧预热、300 帧正式采样、5 次重复和 16 帧轨迹，且不修改 `sk_os` 业务逻辑。`BENCH_MODE=CPU_ONLY|CPU_MATMUL|SCENE_CONTROLLER` 三种镜像均已在 WSL 交叉编译通过。逐帧 CSV 已包含四个 CPU 阶段、轮询/阻塞/墙钟周期、延迟、idle cycle/rate 与固定低优先级后台工作量。当前 `CPU_MATMUL` 明确输出 `CPU_MATMUL_DEFERRED`，不能用于正式比较；CPU 软件链亦为 T12/T13 前的 framework/proxy，不可进入等价性或性能主判据。
- T04--T07 验证：三种模式交叉编译命令返回 0；当前共享 `sdk/axi_ram.mif` 为 `rt_3d`，SHA-256=`72A2FCB7B00ECFF1392B755F09F54C13E820EA01385BBF8B7A8F52C08F8534E8`。
- T08：Scene command FIFO 已参数化为默认 32 项；pipeline 与 multimesh 命令路径均使用 32 项。`scene_cmd_fifo_tb` 覆盖空、满、背压、锁定、完成/错误清空，`CLEAR + 16 DRAW + PRESENT` 的 16 帧 walk 均 PASS。证据：`fpga/verilator/logs/result_scene_cmd_fifo_tb.json`、`result_scene_sk_multimesh_cmd_tb.json`、`result_scene_sk_multimesh_walk_tb.json`。
- T09：Scene IRQ 为电平 IRQ，`0x058/0x05c/0x060` 分别为 enable/sticky status/W1C clear，bit0=render done、bit1=frame done、bit2=error；同周期事件优先于 W1C，未轮询不会丢失，并通过 confreg bit6 路由至 CPU 外部中断。`scene_ctrl_axi_regs_tb` PASS，覆盖 sticky、IRQ enable、W1C 与 IRQ 解除。
- T10：RT-Thread BSP 提供 `scene_ctrl_wait_poll()` 和基于 semaphore 的 `scene_ctrl_wait_irq()`；`rt_3d` 的 `SCENE_CONTROLLER` 镜像默认走阻塞等待。专用 `rt_3d_soc_tb` 已以 blade v4 有效资源通过端到端验证：`FRAME_DONE` 经 CPU→confreg bit6→ext_irq→ISR→semaphore 唤醒，`frame_count` 递增且无 Scene error。
- T11：Scene 只读性能计数器（`0x064..0x084`）已提供 LOAD bytes/AXI 事务、Transform/Cull/Sort/Command cycles、输入/剔除/输出三角形。`scene_ctrl_axi_regs_tb` 验证九个计数器只读保护并读回非零计数；`rt_3d_soc_tb` 的 SoC 证据也通过，最近一次 `make -C fpga/verilator TB=rt_3d_soc_tb run-batch` 为 PASS：`irq_edges=38 cpu_irq_edges=38 reads=190 cmds=258 frames=3 load=760 transform=222 tris=54`。该 TB 还等待 PRESENT 经 vblank 扫描完成，断言 478601 个非黑 DVI 像素，并保存 `sim/frame_output/rt_3d_soc_render.ppm`；不再以启动期黑色的 `_0001.ppm` 作为渲染证据。此为短跑集成验收，不替代 30 帧预热、300 帧×5 的正式性能实验；仿真镜像构建使用 `make -C sdk/software/examples/rt_3d BENCH_MODE=SCENE_CONTROLLER RT3D_SIMULATION=1`，`has_cache=0` 仅在该显式开关下启用。

## C 镜像与 TB 对应

| C 镜像 / 范围 | 构建或运行入口 | 对应 TB | 当前证据与限制 |
|---|---|---|---|
| `scene_sk3d_demo` | `make -C sdk/software/examples/scene_sk3d_demo` | `sketch_scene_soc_tb` | 曾 PASS；仅适用于旧 `scene_sk3d_demo` MIF（`2BCEA...09C1A`），当前已失效。 |
| `sk_os` | `make -C sdk/software/examples/sk_os` | `sketch_soc_os_3d_demo_tb` | 曾 PASS；需以匹配的 MIF 重新构建、重跑后才能引用。 |
| `rt_3d`（T04--T07） | `make -C sdk/software/examples/rt_3d BENCH_MODE=SCENE_CONTROLLER RT3D_SIMULATION=1` | `rt_3d_soc_tb` | C 镜像交叉编译与 SoC 仿真均 PASS；验证 `FRAME_DONE` 的 CPU→ISR→semaphore 阻塞唤醒、性能计数器和 DVI 渲染输出。最近运行：`irq_edges=38 cpu_irq_edges=38 reads=190 cmds=258 frames=3 load=760 transform=222 tris=54`。 |
| T08 FIFO RTL / 无 C API | 不适用 | `scene_cmd_fifo_tb`、`scene_sk_multimesh_cmd_tb`、`scene_sk_multimesh_walk_tb` | FIFO 32 项与 `CLEAR + 16 DRAW + PRESENT`；无 C 镜像依赖。 |
| T09 IRQ 寄存器 C API | `sdk/software/bsp/include/scene_ctrl.h`：`scene_ctrl_irq_enable/status/clear()` | `scene_ctrl_axi_regs_tb`、`rt_3d_soc_tb` | 前者覆盖 `0x058/0x05c/0x060` 的 sticky、IRQ enable、W1C；后者验证 confreg bit6 至 CPU 外部中断的实际链路。 |
| T10 RT-Thread 等待 C 实现 | `sdk/software/bsp/drivers/scene_ctrl_wait.c` + `include/scene_ctrl_wait.h`；`rt_3d/board.c` ISR；`rt_3d/rt_3d.c` 默认 semaphore 等待 | `rt_3d_soc_tb` | 已通过 C 镜像端到端验证 CPU→confreg bit6→ext_irq→ISR→semaphore 的阻塞唤醒。 |
| T11 性能计数器 C 访问 | `sdk/software/bsp/include/scene_ctrl.h` 的 MMIO 基址/读接口（计数器 `0x064..0x084`） | `scene_ctrl_axi_regs_tb`、`rt_3d_soc_tb` | 前者验证九个计数器的只读保护与非零回读；后者以 C 镜像采集 `load=760 transform=222 tris=54`，并验证 DVI 渲染输出。 |
| 其他 Scene RTL / 无 C 镜像 | 不适用 | `sketch_model_3d_tb`、`scene_sk_catalog_switch_tb` | 独立 RTL 回归；不替代任意 C 镜像的 SoC 证据。 |

统一仿真命令：`make -C fpga/verilator TB=<tb> run-batch`。任何 MIF 切换后，所有绑定该镜像的 SoC TB 必须重跑；`FRAME_DUMP_LIMIT(0)` 保持不变。
