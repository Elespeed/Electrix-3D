> 此文档要求表述简洁
当前状态：
- T00：当前工作树无法复核文档声称的 `b224aa0`（本地仅有 `1f05176`、`602bb1f`）；不将“56/56”作为当前基线证据。
- T01：实验协议已完成，待与 T02/T03 一并提交。
- T03：JSONL 校验器和样例已复核：`valid.jsonl` 通过（2 帧），`invalid.jsonl` 按预期拒绝；`sketch_perf_tb` 的预检声明尚无当前日志可复核。
- T02：尚未冻结。2026-08-31 当前工作树的 `sketch_model_3d_tb`、`scene_sk_catalog_switch_tb`、`scene_sk_multimesh_walk_tb`、`sketch_soc_os_3d_demo_tb`、`sketch_scene_soc_tb` 均 PASS。后者使用 WSL 重建的 `scene_sk3d_demo` 镜像；共享 `sdk/axi_ram.mif` SHA-256 为 `2BCEA8D416A38F75F5247E50B507553AE52F4C900609FEB38CE022D863B09C1A`。资产清单 SHA-256 为 `E3011C4C0AF18E031554C96601EBD5A91DF72FC1F03E7AC5ED0822BC7088FA0B`。仍需补齐可复核 Git 基线、冻结元数据与资源/时序证据后才能冻结。
- T04--T07：已新增独立 `sdk/software/examples/rt_3d/` RT-Thread benchmark；固定 30 帧预热、300 帧正式采样、5 次重复和 16 帧轨迹，且不修改 `sk_os` 业务逻辑。`BENCH_MODE=CPU_ONLY|CPU_MATMUL|SCENE_CONTROLLER` 三种镜像均已在 WSL 交叉编译通过。逐帧 CSV 已包含四个 CPU 阶段、轮询/阻塞/墙钟周期、延迟、idle cycle/rate 与固定低优先级后台工作量。当前 `CPU_MATMUL` 明确输出 `CPU_MATMUL_DEFERRED`，不能用于正式比较；CPU 软件链亦为 T12/T13 前的 framework/proxy，不可进入等价性或性能主判据。
- T04--T07 验证：`wsl bash -lc 'cd /mnt/d/FPGA/Electrix-3d/sdk/software/examples/rt_3d && make clean && make BENCH_MODE=CPU_ONLY -j2 && make clean && make BENCH_MODE=CPU_MATMUL -j2 && make clean && make BENCH_MODE=SCENE_CONTROLLER -j2'` 返回 0。尚无专用 `rt_3d` SoC testbench，故未运行 Verilator；不得将本次编译视为运行时性能或等价性证据。最后一次构建使共享 `sdk/axi_ram.mif` 切换为 `rt_3d` 镜像，SHA-256=`72A2FCB7B00ECFF1392B755F09F54C13E820EA01385BBF8B7A8F52C08F8534E8`，旧 SoC 回归结果不能复用。
