# T00 共享 3D 资产迁移验收记录

## 结论

T00 通过验收。3D 源模型、生成资产、资源包和仿真夹具已迁移到仓库根级 `assets/3d/`，生成器、固件和独立 Scene testbench 已改用共享路径。预览帧保留在本地 `assets/3d/previews/`，不再纳入版本控制。

## 已通过项目

- `python -m unittest discover -s tools/3d_gen/tests -v`：56/56 通过。
- `python tools/3d_gen/build_asset_manifest.py`：重复生成后 `manifest.json` 无差异。
- `make -f tools/3d_gen/Makefile ASSET=blade convert`：通过。
- `make -f tools/3d_gen/Makefile ASSET=robotss convert`：通过。
- `make -f tools/3d_gen/Makefile package-br01`：通过，包大小 4512 字节。
- 25 对 MIF/BIN 按 32-bit little-endian 比较完全一致。
- br01 的 3 个 entry 与 JSON、MIF/BIN、catalog header 一致。
- 活动代码和使用文档不存在旧 `sim/test/sketch/3d/assets`、`sim/test/sketch/3d/model`、`tools/3d_gen/frame` 输出路径。
- `git diff --check`：通过。
- `FRAME_DUMP_LIMIT(0)`：未修改。
- `make -C fpga/verilator TB=sketch_model_3d_tb run-batch`：PASS，16 帧、678 个三角形。
- `make -C fpga/verilator TB=scene_sk_catalog_switch_tb run-batch`：PASS，2 次资源切换、31 个三角形。
- `make -C fpga/verilator TB=scene_sk_multimesh_walk_tb run-batch`：PASS，16 帧、5 meshes、464 个三角形。

对应结构化结果位于：

- `fpga/verilator/logs/result_sketch_model_3d_tb.json`
- `fpga/verilator/logs/result_scene_sk_catalog_switch_tb.json`
- `fpga/verilator/logs/result_scene_sk_multimesh_walk_tb.json`

## 已知的非 T00 阻塞项

`sketch_soc_os_3d_demo_tb` 与 `sketch_scene_soc_tb` 均在读取资产前被现有 SoC filelist 阻塞：缺少 `rtl/config_blade2x2sim.h` 与 `rtl/soc_top_lv3_board.sv`。两者的结构化结果均为 compile failure，错误与本次迁移后的资产路径无关。本任务不越界补建 SoC 顶层；该问题交由后续 SoC/RTOS 阶段处理后重跑。

## 版本控制边界

- `assets/3d/sources`、`generated`、`packages`、`fixtures` 和 `manifest.json` 纳入版本控制。
- `assets/3d/previews` 仅为可再生成输出，保留在工作区并由 `.gitignore` 忽略。
- `legacy_v3` 中的字节差异版本继续保留，完成消费方哈希审计前不得按同名文件去重。
