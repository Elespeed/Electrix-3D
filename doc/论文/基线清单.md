# 论文整改基线清单（T02）

本清单固定后续实验与论文数据的可复现起点。唯一认可的代码基线是提交 `b224aa0`（`chore: migrate shared 3d assets`，分支 `research`，当前 `origin/research` 同指该提交）。除非后续任务明确建立新基线，不得用工作区快照或历史综合目录替代该提交。

## Git、目录树与工作区

| 项目 | 冻结记录 |
|---|---|
| 基线提交 | `b224aa0` |
| 基线提交树 | `git ls-tree -r --name-only b224aa0` 共 1080 个路径（以命令输出为准） |
| 基线资产根 | `assets/3d/`，包括 `sources/`、`generated/`、`packages/`、`fixtures/`、`manifest.json` |
| 基线工作区要求 | 实验运行前应为干净工作区；必须记录 `git status --short --untracked-files=all`，不得把未提交文件混入结果 |
| 本次核对时工作区 | 非干净：存在 `doc/sketch/`、`doc/论文/存档/`、`doc/论文/实验协议.md`、`doc/论文/整改行动.md`、`doc/论文/论文大纲.md`、`experiments/`、`tools/validate_3d_jsonl.py` 等未跟踪内容；这些不属于 `b224aa0` 基线 |

复核命令：

```powershell
git rev-parse --verify b224aa0^{commit}
git show --no-renames --stat --oneline b224aa0
git ls-tree -r --name-only b224aa0
git status --short --untracked-files=all
```

## 资产清单与哈希

基线资产清单为 `assets/3d/manifest.json`。本次核对得到 SHA-256：

```text
E3011C4C0AF18E031554C96601EBD5A91DF72FC1F03E7AC5ED0822BC7088FA0B  assets/3d/manifest.json
```

复核命令：

```powershell
Get-FileHash assets/3d/manifest.json -Algorithm SHA256
python tools/3d_gen/build_asset_manifest.py
git diff --exit-code -- assets/3d/manifest.json
```

`build_asset_manifest.py` 重复生成后应无差异；若哈希改变，必须先说明生成器、输入资产或工具链变化，并重新建立经过验收的基线，不得静默覆盖本记录。

## T00 验收证据

证据来源：`doc/论文/00_项目管理/T00_验收记录.md` 及对应结构化结果 JSON。以下结果均以 Verilator 5.048、无 VCD（`capture_mode=disabled`）记录为准：

| TB | 结果 | 关键通过标记 |
|---|---|---|
| `sketch_model_3d_tb` | PASS，compile/load/run 均成功，rc=0 | `frames=16 clear=16 triangles=678 presents=16` |
| `scene_sk_catalog_switch_tb` | PASS，compile/load/run 均成功，rc=0 | `clear=2 tri=31 present=2` |
| `scene_sk_multimesh_walk_tb` | PASS，compile/load/run 均成功，rc=0 | `frames=16 meshes=5 clear=16 tri=464 present=16 reads=464 capture=32` |

可重复命令（项目规定的 Verilator 入口）：

```powershell
make -C fpga/verilator TB=sketch_model_3d_tb run-batch
make -C fpga/verilator TB=scene_sk_catalog_switch_tb run-batch
make -C fpga/verilator TB=scene_sk_multimesh_walk_tb run-batch
```

结果文件分别为：

```text
fpga/verilator/logs/result_sketch_model_3d_tb.json
fpga/verilator/logs/result_scene_sk_catalog_switch_tb.json
fpga/verilator/logs/result_scene_sk_multimesh_walk_tb.json
```

## SoC 验证边界

以下两项不是 T00 失败，而是现有 SoC filelist 的 compile blocker；均在读取迁移后资产前停止：

| TB | 结果 | 首个有意义错误 |
|---|---|---|
| `sketch_soc_os_3d_demo_tb` | FAIL，compile/load/run 未执行，compile rc=1 | 缺少 `rtl/config_blade2x2sim.h`；缺少 `rtl/soc_top_lv3_board.sv` |
| `sketch_scene_soc_tb` | FAIL，compile/load/run 未执行，compile rc=1 | 缺少 `rtl/config_blade2x2sim.h`；缺少 `rtl/soc_top_lv3_board.sv` |

可重复命令：

```powershell
make -C fpga/verilator TB=sketch_soc_os_3d_demo_tb run-batch
make -C fpga/verilator TB=sketch_scene_soc_tb run-batch
```

对应结果文件为 `fpga/verilator/logs/result_sketch_soc_os_3d_demo_tb.json` 与 `fpga/verilator/logs/result_sketch_scene_soc_tb.json`。在两个缺失文件恢复并完成重新编译前，不得声称 SoC/RTOS 端到端通过，也不得将该 compile blocker 归因于资产迁移。

## 综合报告与仿真不变量

仓库中的 Vivado/综合报告只能作为历史参考，不能作为本基线的当前资源、时序、功耗或性能主张。正式论文数据必须记录生成日期、工具版本、约束、顶层和完整报告路径；当前待补，不能用历史数字替代。

所有受测 testbench 的 `FRAME_DUMP_LIMIT(0)` 必须保持不变。禁止为缩短运行时间而修改源文件中的该参数；如需一次性限制输出，只能使用验证流程允许的临时参数覆盖，并在结果中明确记录，且不得改变基线文件。

## 待补信息与解除条件

1. 清理或隔离工作区未跟踪文件，并记录干净状态后再运行正式实验。
2. 补齐并审计 `rtl/config_blade2x2sim.h`、`rtl/soc_top_lv3_board.sv` 的来源、版本和许可，重新运行两项 SoC TB。
3. 保存 SoC 重新编译后的结构化结果、首个错误/最后检查点及软件镜像（`sdk/axi_ram.mif`）哈希。
4. 补充当前综合/时序/功耗报告及其工具、约束和日期；在此之前仅标为“历史参考”。
5. 若 `assets/3d/manifest.json` 或基线提交发生变化，新增版本化基线记录，不直接改写本清单的冻结事实。

