# 论文整改基线清单（T02）

本清单记录冻结候选的可复核身份与验证证据。当前记录对应 `research` 的 HEAD `1f05176a32003d0f9df82ac264a3014fc4db57a9` 加上下面列出的未提交恢复文件和软件镜像；它不是正式冻结的论文基线。只有工作区清理、证据补齐并提交后，才可将其升级为正式基线；不得引用不存在的 `b224aa0` 或历史综合目录。

## Git、目录树与工作区

| 项目 | 冻结记录 |
|---|---|
| Git HEAD / 分支 | `1f05176a32003d0f9df82ac264a3014fc4db57a9` / `research` |
| 代码状态 | 非干净；`rtl/sub_system/graph_system.sv` 已从 `602bb1f` 恢复（仅多结尾换行），尚未提交 |
| 软件镜像 | `sdk/axi_ram.mif`：`scene_sk3d_demo`，SHA-256 `2BCEA8D416A38F75F5247E50B507553AE52F4C900609FEB38CE022D863B09C1A`，334224 字节 |
| 基线资产根 | `assets/3d/`，包括 `sources/`、`generated/`、`packages/`、`fixtures/`、`manifest.json` |
| 正式冻结要求 | 实验运行前工作区必须干净，并记录 `git status --short --untracked-files=all`；不得把未提交文件混入正式数据 |

复核命令：

```powershell
git rev-parse HEAD
git branch --show-current
git status --short --untracked-files=all
Get-FileHash sdk/axi_ram.mif -Algorithm SHA256
```

## 资产清单与哈希

基线资产清单为 `assets/3d/manifest.json`。本次核对得到 SHA-256：

```text
E3011C4C0AF18E031554C96601EBD5A91DF72FC1F03E7AC5ED0822BC7088FA0B  assets/3d/manifest.json
```

复核命令：

```powershell
Get-FileHash assets/3d/manifest.json -Algorithm SHA256
python tools/3d_gen/build_asset_manifest.py
git diff --exit-code -- assets/3d/manifest.json
```

`build_asset_manifest.py` 重复生成后应无差异；若哈希改变，必须先说明生成器、输入资产或工具链变化，并重新建立经过验收的基线，不得静默覆盖本记录。

## 当前 Verilator 回归证据

以下结果均在当前候选工作树上复跑，以 Verilator 5.048、无 VCD（`capture_mode=disabled`）记录为准：

| TB | 结果 | 关键通过标记 |
|---|---|---|
| `sketch_model_3d_tb` | PASS，compile/load/run 均成功，rc=0 | `frames=16 clear=16 triangles=678 presents=16` |
| `scene_sk_catalog_switch_tb` | PASS，compile/load/run 均成功，rc=0 | `clear=2 tri=31 present=2` |
| `scene_sk_multimesh_walk_tb` | PASS，compile/load/run 均成功，rc=0 | `frames=16 meshes=5 clear=16 tri=464 present=16 reads=464 capture=32` |

可重复命令（项目规定的 Verilator 入口）：

```powershell
make -C fpga/verilator TB=sketch_model_3d_tb run-batch
make -C fpga/verilator TB=scene_sk_catalog_switch_tb run-batch
make -C fpga/verilator TB=scene_sk_multimesh_walk_tb run-batch
```

结果文件分别为：

```text
fpga/verilator/logs/result_sketch_model_3d_tb.json
fpga/verilator/logs/result_scene_sk_catalog_switch_tb.json
fpga/verilator/logs/result_scene_sk_multimesh_walk_tb.json
```

## SoC 回归

`graph_system.sv` 恢复后，两项 SoC TB 均已 compile/load/run 成功。`sketch_scene_soc_tb` 使用上述 `scene_sk3d_demo` 镜像；若将共享 MIF 切换为其他镜像，必须重新运行该 TB，不能复用此结果。

| TB | 结果 | 关键通过标记 |
|---|---|---|
| `sketch_soc_os_3d_demo_tb` | PASS，compile/load/run 均成功 | `[sketch_soc_os_3d_demo_tb] PASS` |
| `sketch_scene_soc_tb` | PASS，compile/load/run 均成功 | `PASS frames=3 reads=186 cmds=303` |

可重复命令：

```powershell
make -C fpga/verilator TB=sketch_soc_os_3d_demo_tb run-batch
make -C fpga/verilator TB=sketch_scene_soc_tb run-batch
```

对应结果文件为 `fpga/verilator/logs/result_sketch_soc_os_3d_demo_tb.json` 与 `fpga/verilator/logs/result_sketch_scene_soc_tb.json`。这些是功能回归证据，不是协议所需的 300 帧 × 5 次正式性能实验。

## 综合报告与仿真不变量

仓库中的 Vivado/综合报告只能作为历史参考，不能作为本候选的当前资源、时序、功耗或性能主张。当前 Verilator 使用默认 `CONFIG_PROFILE=remote`；正式论文数据还必须记录 RTL 宏、CPU/RTL 时钟、工具版本、约束、顶层、完整资源/时序报告路径和生成日期。

所有受测 testbench 的 `FRAME_DUMP_LIMIT(0)` 必须保持不变。禁止为缩短运行时间而修改源文件中的该参数；如需一次性限制输出，只能使用验证流程允许的临时参数覆盖，并在结果中明确记录，且不得改变基线文件。

## 待补信息与解除条件


1. 清理或提交当前工作区修改（尤其是 `rtl/sub_system/graph_system.sv`、场景镜像及本清单），然后记录干净状态。
2. 补充当前综合/时序/功耗报告及其工具、约束、顶层和日期；在此之前仅标为“历史参考”。
3. 冻结协议所需的配置哈希、完整模型/控制组清单、时钟与统计口径，并执行正式的 300 帧 × 5 次实验；当前五项 TB PASS 不能替代它们。
4. 若 Git 提交、`sdk/axi_ram.mif`、`assets/3d/manifest.json` 或配置发生变化，新增版本化基线记录，不直接改写已冻结的历史事实。
